# AGENTS.md

本项目的最高纪律。任何在此仓库工作的 agent 必须遵守。

## 核心铁律：实现忠于设计

`docs/` 是唯一真相源（single source of truth），代码是它的投影：

- **`docs/schema/`** —— 所有 schema 的权威定义（字段名、类型、枚举、不变式）。
- **`docs/nodes/`** —— 所有处理节点的权威定义（职责、入参、输出、交互）。
- **`docs/ARCHITECTURE.md` / `FOUNDATION.md` / `EVALUATION.md` / `ROADMAP.md`** —— 架构与路线。

写代码时，字段、命名、状态机、约束一律**逐字段照抄文档**，不得凭记忆发挥。
代码与文档不一致 = bug，无论测试是否通过（"绿"不等于"忠于设计的绿"）。

## 发现设计有问题时的顺序（不可颠倒）

在实现中若判断设计本身有错、缺失或自相矛盾：

1. **停手**，不要先改代码"绕过"或"就地补丁"。
2. **先改设计**：提出对 `docs/` 的修订方案，说明理由与影响面，按下方 `.cursor/rules/propose-before-writing.mdc` 的约定获得同意后落盘。
3. **再改代码**：让实现对齐修订后的文档。
4. 若设计正确而代码偏离：直接修代码对齐文档，无需改文档。

一句话：**先文档、后代码；文档变更走提案，代码永远追随文档。**

## 配套约定

- 落盘纪律见 `.cursor/rules/propose-before-writing.mdc`（未经要求不擅自改文件）。
- 提交/推送仅在被明确要求时进行。
