---
description: 默认只在对话里提案，用户明确要求才落盘
alwaysApply: true
---

# 提案优先，落盘需授权

本项目是设计协作：先讨论、拷打，再冻结。**禁止未经用户明确要求直接修改/写入任何文件**（尤其 `docs/` 下的设计文档）。

## 默认行为

- 默认在**对话里**给出提案：schema、字段、图、方案，用文本/代码块呈现，供用户拷打。
- **不要**擅自调用写文件工具落盘（StrReplace / Write / EditNotebook 等）。

## 何时才可落盘

仅当用户发出明确的落盘指令，例如：「落盘」「冻结」「写进文档」「改 §X」「更新」「记下来」。

- 指令针对哪部分，就只改哪部分，不顺手改其它。
- 落盘后简述改了哪些位置。

## 例子

```
用户：SceneScript 该包含什么
✅ 在对话里列出提案 + 需拍板的岔路，等用户确认
❌ 直接把 SceneScript 写进 ARCHITECTURE.md

用户：OK，冻进 §11.3
✅ 此时才写文件，并说明改动位置
```
